/*
 * This file is part of VIB (Virtual Interactive Behaviour).
 */
package vib.core.util.enums;

/**
 *
 * @author Andre-Marie Pez
 *
 *
 * I don't like enums... but it's better than Strings that eveybody use over much
 * (I prefer "static final int"s that are lighter)
 */
public enum Side {
    LEFT,
    RIGHT,
    BOTH;
}
